This directory keeps historical or superseded analysis scripts that are not part
of the current paper reproduction path.

Active paper-facing entrypoints now live in the top-level `scripts/` directory:

- `reproduce_paper_bundle.py`
- `generate_paper_table1.py`
- `generate_spatial_score_figure.py`
- `generate_jammer_curve_sim.py`

Archive candidates were moved here because they are older variants, report-only
helpers, or exploratory versions that would otherwise clutter `git status`.
