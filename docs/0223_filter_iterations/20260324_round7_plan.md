# 2026-03-24 Round 7 Multi-Agent Plan

## Reference Status

Round 7 starts from two locked references:

- round5 lane4 remains the promoted central-window deployed baseline
- round6 lane3 `sign_veto_poslift_r0p10` is the best robustness-recovery branch

Round6 lane4 is not the winning recovery branch and is not the base for new work.

## Team Roles

### Supervisor

Owns:

- lane order
- pass/fail gates
- promotion judgment

Does not rewrite experiment code while worker lanes are active.

### Engineer A: Block7 Persistence

Owns:

- block7-focused scripts
- block7-focused reports and artifacts

Search axis:

- negative-family persistence only
- same-`VR` negative-family support
- no new preprocessing

### Engineer B: Block6 Persistence

Owns:

- block6-focused scripts
- block6-focused reports and artifacts

Search axis:

- positive-family persistence only
- same-anchor / same-`VL` lift stability
- no block7-specific rewrites

### Engineer C: Hybrid Integrator

Owns:

- hybrid scripts
- hybrid reports and artifacts

This lane starts only after Engineer A and Engineer B each produce a passing
local winner.

### Validator

Owns:

- fixed validation matrix
- pass/fail judgment
- final tracked memo for each round

## Lane Order

### Lane 7.1: Block7 Persistence First

Base:

- freeze round6 lane3 `sign_veto_poslift_r0p10`

Search target:

- block7 negative-family persistence
- same-`VR` anchored negative-family promotion or veto

This is the smallest next step because round6 showed:

- sign consistency is partly solved
- block7 still lacks strict top1 persistence

### Lane 7.2: Block6 Persistence

Base:

- freeze the best passing lane 7.1 variant
- if lane 7.1 fails, keep round6 lane3 frozen

Search target:

- block6 positive-family persistence
- same-`VL` / same-anchor lift retention

### Lane 7.3: Hybrid Integration

Base:

- combine the smallest passing pieces from 7.1 and 7.2 only

This lane is for compatibility, not a new broad search.

## Round7-L1 Minimum Pass Gate

- `valid_cases = 4`
- `physical_count = 4`
- central `delta_tau_mae_ms <= 0.060`
- central hard-case `delta_tau_mae_ms <= 0.080`
- central hard-case `win_rate = 1.0`
- window mean/std `<= 0.12`
- window max/std `<= 0.22`
- setting `pass_rate >= 0.50`
- `block6 hit@1 >= 3/5`
- `block7 hit@1 >= 2/5`
- `block7 hit@3 >= 4/5`
- `block7` stays in the rescued negative regime at the central window

Immediate fail:

- any central regression beyond the caps above
- `block6 hit@1 < 3/5`
- `block7 hit@1` stays at `1/5`
- `pass_rate < 0.50`

## Round7-L2 Minimum Pass Gate

- preserve all lane 7.1 central caps
- `pass_rate >= 0.60`
- preserve lane 7.1 block7 gain
- `block6 hit@1 >= 4/5`
- `block6 hit@3 = 5/5`
- window mean/std `<= 0.11`
- window max/std `<= 0.20`

## Promotion Rule

Do not promote a round7 lane unless it improves hard-case persistence without
losing the promoted central solution.

Average-only gains do not count.
