# 0223 Filter Iterations

This directory stores supervisor and validator notes for continued 0223
`delta_tau` improvement attempts after the first validated baseline.

Use this folder for:

- supervisor priority memos
- iteration summaries
- validator accept or reject notes

Do not store raw run outputs here. Raw outputs remain under `results/`.
